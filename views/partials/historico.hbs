<div>
    <h2 style="color:crimson;">Historial de Productos</h2>

    {{#if DB_PRODUCTOS}}
    <div class="table-responsive">
        <table class="table table-dark" id="product_table">
    
        </table>
    </div>
    {{else}}
        <h3 class="alert alert-warning"> No se encontraron productos</h3>
    {{/if}}
</div>